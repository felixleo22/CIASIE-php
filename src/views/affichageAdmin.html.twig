{% extends 'base.html.twig' %}

{% block content %}
    <table>
        <tr>
            <th>login</th>
        </tr>
    {% for admins in admin %}
        <tr>
            <td>{{ admin.login }}</td>
            <td> <a href ="/admin/modifier/{{ admin.id }}" >Modifier</a> </td>
            <td> <a href ="/admin/supprimer/{{ admin.id }}" >Suprimer</a> </td>
        </tr>
    {% endfor %}
    </table>
{% endblock %}
