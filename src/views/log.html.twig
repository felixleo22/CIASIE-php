{%  extends 'base.html.twig' %}

{% block content %}

    <table>
        <thead>
        <tr>
            <th>Login</th>
            <th>Mdp</th>
            <th>status</th>
            <th>Opération</th>
        </tr>
        </thead>
        <tbody>
        {% for admin in admins if admin.super == 0%}
                <td>{{ admin.login }}</td>
                <td>{{ admin.mdp }}</td>
                <td>{{ admin.super }}</td>
                <td>
                    <form action="{{path_for('execSupprAdmin', {id: admin.id})}}" method="post">
                        <input type="submit" class="btn btn-danger" value="Supprimer">
                    </form>
                </td>
                <td>
                    <form action="{{path_for('execModifAdmin', {id: admin.id})}}" method="get">
                        <input type="submit" class="btn btn-success" value="Modifier">
                    </form>
                </td>
            </tr>

        {% endfor %}


        </tbody>
    </table>


{% endblock %}
